struct myStructure {
    //could be called BlackTail if you wanted
  int myNum;
  char myLetter;
};

int main() {
  struct myStructure s1; 
  // s1 is used to access information in struct myStructure
  return 0;
}